logging:
  level:
    root: info

spring:
  datasource:
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/book_library
  jpa:
    database: mysql
    show-sql: true
    generate-ddl: true
    #hibernate:
      #ddl-auto: create
book-library:
  some:
    parameter: "some real value"
    other:
      parameter: "another real value"
  discounts:
    discount-example-1: 10
    discount-example-2: 15
    discount-example-3: 20

